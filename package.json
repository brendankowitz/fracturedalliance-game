{
  "name": "fractured-alliance",
  "private": true,
  "scripts": {
    "dev": "pnpm -F @fa/web dev",
    "build": "pnpm --filter @fa/web... build",
    "build:desktop": "pnpm -F @fa/desktop build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.6"
  }
}
